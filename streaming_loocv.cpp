// Archived LOOCV firmware. The default Arduino build compiles every .cpp file
// in the repository root, so this file must stay inert unless it is moved out
// of the sketch folder or explicitly reactivated for a separate build.
